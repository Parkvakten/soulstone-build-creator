import { FilterskillsPipe } from './filterskills.pipe';

describe('FilterskillsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterskillsPipe();
    expect(pipe).toBeTruthy();
  });
});
